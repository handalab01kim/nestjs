import { TransformDateInterceptor } from './transform-date.interceptor';

describe('TransformDateInterceptor', () => {
  it('should be defined', () => {
    expect(new TransformDateInterceptor()).toBeDefined();
  });
});
